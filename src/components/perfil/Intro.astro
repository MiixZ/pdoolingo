---

---

<button id="login" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded"> Iniciar sesión con Google </button>

<script>
    const { signIn } = await import("auth-astro/client")

    const login = document.getElementById("login");

    login?.addEventListener("click", async () => {
        await signIn("google")
    });
</script>