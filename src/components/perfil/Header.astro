---
interface Props {
    email: string;
    name: string;
    image: string;
}

const { email, name, image } = Astro.props;
---

<div class="flex relative flex-row items-center p-7 justify-center bg-slate-500 rounded-lg w-3/4">
    <img src={image} alt={name} class="w-12 h-12 rounded-full" />
    <div class="ml-4">
        <h2 class="text-2xl font-semibold">¡HOLA, {name}!</h2>
        <p class="text-sm text-gray-800">{email}</p>
    </div>
</div>